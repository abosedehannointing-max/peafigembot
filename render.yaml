services:
  - type: worker
    name: telegram-member-adder
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python add_members.py
    envVars:
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
      - key: PHONE_NUMBER
        sync: false
